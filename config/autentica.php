<?php

// session_start(); //iniciando a sessão do usuário
    
//     require_once "../model/banco.php"; //requerindo o arquivo ../model/banco.php
//      if(isset($_POST['Acessar']) && $_POST['Acessar'] === 'Acessar'){ //criando uma estrutura de decisão pra saber se o botão validar foi precionado, recebendo nome do botão através da variavel super global $_POST, utilizando od && "e-comercial", pra testar se o value do botão validar é identico a "validar
//          $cpf = $_POST['cpf']; //criando uma variavel com o nome de login e atribuindo a ela a varivarel super global $_POST pasando como parametro o email
//          $senha = $_POST['senha']; //criando uma variavel com o nome senha e atribuindo a ela o valor da variavel super global $_POST e passando como parametro a senha
        
//          $aut = autentica($cpf,$senha,$total);
//         if ($total == 0) {
//             echo "<meta http-equiv='refresh' content='0;URL=../index.php'">
//             header("Location:../index.php");
//         }else{
//             foreach ($aut as $valores){
//                 header("Location:../view/ambienteAluno.html");
//             }
//         }
//      }

    // require_once "conexao.php";
    // if(isset($_POST['validar']) && $_POST['validar'] === 'validar'){
    //     $cpf = $_POST['cpf'];
    //     $senha = $_POST['senha'];
        
    //     $aut = autentica($cpf,$senha,$total);
    //     if ($total == 0) {
    //         //echo "<meta http-equiv='refresh' content='0;URL=../index.php'">
    //         header("Location:../index.html");
    //         exit;
    //     }else{
    //         foreach ($aut as $valores){
    //             header("Location:../view/ambienteAluno.html");
    //         }
    //     }
    // }

     

    

?>